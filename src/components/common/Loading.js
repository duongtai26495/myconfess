import React from 'react'
import loading from '../../assets/images/loading.gif'

const Loading = () => {
  return (
    <img src={loading} className="m-auto loading_gif" />
  )
}

export default Loading